# รายงานความพร้อมโปรเจกต์ ePostal

วันที่ตรวจ: 29 มิถุนายน 2026  
สถานะรวม: พร้อมสำหรับตรวจทานและรวมงาน แต่ยังไม่แนะนำให้ deploy ทันที

## สรุปสั้น

โปรเจกต์อยู่ในสภาพดีขึ้นมากหลังจากจัดการงานทดสอบและความสะอาดของไฟล์แล้ว ตอนนี้ระบบสร้างเว็บได้สำเร็จ และชุดทดสอบหลักผ่านครบ

ยังมีงานค้างเดิมหลายกลุ่มที่ควรแยกตรวจทีละชุดก่อนปล่อยใช้งานจริง โดยเฉพาะงาน backend, auth, เอกสารส่งต่อ, และโฟลเดอร์งานจาก agent หลายชุด

## ผลตรวจล่าสุด

| รายการตรวจ | ผลลัพธ์ |
| --- | --- |
| สร้างเว็บ frontend | ผ่าน |
| ทดสอบหลัก | ผ่าน 22/22 |
| ทดสอบผ่านหน้าเว็บ | ผ่าน 11/11 |
| ตรวจปัญหารูปแบบไฟล์ | ผ่าน |
| ทดสอบไม่ออกเว็บนอก | ผ่าน |
| ตรวจไฟล์ lock แพ็กเกจ | สะอาด ไม่มีการเปลี่ยนค้าง |

## สิ่งที่แก้แล้ว

- ป้องกันชุดทดสอบไม่ให้พยายามติดต่อ Google ระหว่างรันในเครื่อง
- ปรับการทดสอบหน้าเว็บให้เหมาะกับเครื่องพัฒนา ไม่เอาเวลาเริ่มระบบครั้งแรกไปตัดสินแรงเกินไป
- แก้ไฟล์ทดสอบตัวอย่างที่เดิมเปิดเว็บ Playwright ข้างนอก ให้ตรวจหน้า ePostal แทน
- เพิ่มการละเว้น `.vite-cache/` เพื่อไม่ให้ไฟล์ cache เข้ามาปนในงาน
- คืน `package-lock.json` กลับ เพราะเป็นการเปลี่ยนใหญ่ที่ไม่มีเหตุผลรองรับจาก `package.json`

## กลุ่มงานที่ควรเอาเข้า

งานกลุ่มนี้มีผลตรวจรองรับแล้ว และควรถูกพิจารณาเป็นชุดแรก:

- การปรับชุดทดสอบให้รันในเครื่องได้โดยไม่ต้องพึ่งเน็ต
- การตั้งค่า Playwright ให้เปิดเว็บในโหมดทดสอบ
- การปรับ Vite ให้ไม่เปิดทางต่อ Google ระหว่างโหมดทดสอบ
- การแทนที่ test ตัวอย่างด้วย test ของ ePostal จริง
- test ใหม่ที่ยืนยันการเก็บ token ของผู้ใช้และการออกจากระบบ

## กลุ่มงานที่ควรตรวจเพิ่มก่อนเอาเข้า

งานกลุ่มนี้มีแนวโน้มดี แต่ควรอ่าน diff และทวนผลกระทบก่อนรวม:

- การเปลี่ยนระบบ auth ใน frontend
- การเปลี่ยนระบบ health check ใน backend
- การเพิ่ม endpoint สำหรับตั้งค่าการติดตาม uptime
- การเปลี่ยน `frontend/.env.example`
- การเปลี่ยน `FeedbackWidget` ให้ใช้ข้อมูลผู้ใช้จาก store

## กลุ่มงานที่ยังไม่ควรแตะมั่ว

กลุ่มนี้ดูเป็นงานค้างจาก agent/orchestrator หรือเอกสารประกอบ ควรตัดสินแยก:

- `.agents/orchestrator/`
- `.agents/sentinel/`
- `.agents/teamwork_preview_*`
- `.agents/skills_backup_1774591545339`
- `.agents/memory/HANDOFF.md`
- `.agents/memory/MASTER_DATA_GUIDE.md`
- `.agents/memory/ROADMAP.md`
- `epostal-master-blueprint.md`

## ความเสี่ยงที่ยังเหลือ

1. ยังไม่ได้ยืนยัน backend กับ Google Apps Script จริงในรอบนี้
2. ยังไม่ได้ deploy
3. มีไฟล์ค้างจำนวนมากใน `.agents` ซึ่งอาจเป็นงานระบบช่วยทำงาน ไม่ควรรวมทั้งหมดทันที
4. health check ใหม่เข้มขึ้น ถ้าค่า config บน production ไม่ครบ อาจขึ้นสถานะไม่พร้อม
5. มี warning จาก Git เรื่องไฟล์ config ส่วนตัวของ Windows แต่ไม่กระทบผลทดสอบของโปรเจกต์

## คำแนะนำก่อน deploy

1. ตรวจ diff ของ backend และ auth แบบเจาะจง
2. เลือกไฟล์ที่จะรวมเป็นชุดแรก เฉพาะงาน test/readiness ที่ผ่านแล้ว
3. ตรวจค่า config จริงบน production เช่น secret, admin email, backup folder
4. รันคำสั่งตรวจ backend หรือ `clasp status` ก่อน deploy
5. deploy หลังจากไฟล์ที่จะรวมสะอาดและไม่มีงาน agent ปน

## ลำดับงานถัดไปที่แนะนำ

1. Commit ชุด readiness/test cleanup ก่อน
2. Review backend health/auth ต่อ
3. Review เอกสาร handover ต่อ
4. ค่อยตัดสิน deploy หลัง backend config พร้อม

## ข้อสรุป

โปรเจกต์พร้อมสำหรับขั้นตอนตรวจทานก่อนรวมงานแล้ว แต่ยังไม่ควร deploy แบบรวบทุกอย่าง เพราะมีงานค้างหลายกลุ่มปนกันอยู่

ทางที่ปลอดภัยที่สุดคือรวมเฉพาะชุดที่ผลตรวจผ่านก่อน แล้วค่อยไล่งาน backend/auth/เอกสารทีละชุด
