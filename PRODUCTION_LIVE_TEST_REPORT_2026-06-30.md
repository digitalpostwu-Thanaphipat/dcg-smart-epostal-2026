# รายงานทดสอบระบบจริง ePostal

วันที่ตรวจ: 30 มิถุนายน 2026  
ระบบจริง: Google Apps Script Web App  
Deployment: `AKfycbz3A7-mdN49qYnSnNjo5vyNaHbZL3KMoMRZq0UL8UU91fUhrcmOnNOAtjK-9gAyjuEilg @248`

## สรุปสถานะ

ระบบพร้อมทดสอบใช้งานจริงแบบจำกัดวงแล้ว โดยจุดหลักที่ตรวจผ่านคือ:

- ลิงก์ติดตามไปรษณีย์ภัณฑ์แบบสาธารณะเปิดได้จริง
- หน้า Admin เข้าสู่ระบบด้วย OTP ได้จริง
- บัญชี `digitalpost.wu@gmail.com` ได้สิทธิ์ `Admin`
- ระบบอ่านรายชื่อผู้ใช้งานจากชีทโปรเจกต์ได้
- ระบบอ่านรายชื่อหน่วยงานได้
- ระบบค้นหารายการพัสดุจากข้อมูลจริงได้

## ผลทดสอบระบบจริง

| รายการตรวจ | ผล | รายละเอียด |
| --- | --- | --- |
| ลิงก์ติดตามสาธารณะ | ผ่าน | เปิด URL ที่มี `publicTrack=1` แล้วเข้าหน้า "ติดตามไปรษณีย์ภัณฑ์" ได้ |
| ช่องค้นหาสาธารณะ | ผ่าน | มีช่องค้นหา มีรายชื่อหน่วยงาน และกดค้นหาได้ |
| Login Admin ด้วย OTP | ผ่าน | OTP ล่าสุดยืนยันสำเร็จ |
| สิทธิ์บัญชี Admin | ผ่าน | `digitalpost.wu@gmail.com` ได้สิทธิ์ `Admin` |
| ข้อมูลผู้ใช้ | ผ่าน | อ่านรายชื่อผู้ใช้งานได้ 15 คน |
| ข้อมูลหน่วยงาน | ผ่าน | อ่านรายชื่อหน่วยงานได้ 62 หน่วยงาน |
| ค้นหาพัสดุ | ผ่าน | ค้นหาข้อมูลจริงได้ 17 รายการ |
| Unit test | ผ่าน | `npm run test:unit` ผ่าน 22/22 |
| Build frontend | ผ่าน | `npm run build --prefix frontend` ผ่าน |
| Deploy backend | ผ่าน | `clasp push` และ `clasp redeploy` สำเร็จ |

## บัญชีที่ใช้ทดสอบ

- Email: `digitalpost.wu@gmail.com`
- ชื่อ: ธนพิพัฒน์ สัมพันธมาศ
- สิทธิ์: Admin
- หน่วยงาน: ส่วนอำนวยการและสารบรรณ

## สิ่งที่แก้ในรอบนี้

- แก้การเปิดหน้าติดตามสาธารณะบน Apps Script ให้รู้จัก `publicTrack=1`
- เปลี่ยนวิธีตรวจ URL ให้รองรับ iframe ของ Google Apps Script
- ตัดปัญหา Apps Script อ่าน template ผิดจากไฟล์ frontend ขนาดใหญ่
- ปรับ `backend/Code.gs` เล็กน้อยเพื่อให้ syntax เข้ากับ Apps Script

## จุดที่ยังควรตามต่อ

1. Console ยังเตือน `VITE_GAS_URL is missing in Production`
   - ตอนนี้ไม่ขวางการใช้งานผ่าน Google Apps Script
   - ควรเก็บเป็นงานปรับ config เพื่อลดเสียงเตือนและกันปัญหาเมื่อเปิดใช้งานนอก Apps Script

2. Service worker ยังเตือนว่าไฟล์ `sw.js` ถูกส่งเป็น `text/html`
   - ตอนนี้ไม่ขวางการ login, tracking, หรือค้นหาข้อมูล
   - ควรแก้ก่อนประกาศรองรับ offline/PWA แบบเต็ม

3. ยังไม่ได้ทดสอบเขียนข้อมูลจริง
   - รอบนี้ทดสอบแบบอ่านข้อมูลจริงเป็นหลัก
   - ก่อนใช้งานเต็มควรทดสอบเพิ่มพัสดุ 1 รายการ, เปลี่ยนสถานะ, แล้วตรวจผลในชีทจริง

## สถานะไฟล์ในเครื่อง

ยังมีไฟล์ที่แก้แล้วแต่ยังไม่ได้ commit:

- `backend/Code.gs`
- `frontend/src/App.tsx`

ควร commit หลังตรวจทานรอบสุดท้ายและตัดสินใจว่าจะเก็บ warning 2 จุดไว้เป็นงานถัดไปหรือแก้ต่อทันที

## เอกสารที่เกี่ยวข้อง

- `QUALITY_GATES.md` อัปเดตผลตรวจ production ล่าสุดแล้ว
- `DECISION_LOG_UPDATE_2026-06-30.md` บันทึก decision ล่าสุดแยกไว้ เพราะ `DECISION_LOG.md` เดิมมีปัญหา encoding
